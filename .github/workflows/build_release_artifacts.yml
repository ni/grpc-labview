name: Build Release artifacts

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build_release_artifacts:
    env:
      STAGING_PATH: ${{ runner.temp }}\grpc-labview-staged-artifacts
    name: Download and Build artifacts
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2

    - uses: actions/download-artifact@v3
      id: download_artifacts
      with:
        path: ${{ runner.temp }}\grpc-labview-artifacts

    - name: Stage Artifacts
      run:
       python ${{runner.workspace}}\grpc-labview\build-it\stage_artifacts.py --downloaded_path ${{ steps.download_artifacts.outputs.download-path }} --staging_path ${{ env.STAGING_PATH}}
    
    - name: Build VI Packages
      run:
       python ${{runner.workspace}}\grpc-labview\build-it\build.py --target All --pathToBinaries ${{ env.STAGING_PATH }}\TopLevelDlls

    - if: startsWith(github.ref, 'refs/tags/v')
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        name: Verison ${{ github.ref }}
        draft: true

    - if: startsWith(github.ref, 'refs/tags/v')
      name: upload release artifacts
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{runner.workspace}}/grpc-labview/labview source/Builds
          asset_name: grpc-laview.zip
          asset_content_type: application/zip
